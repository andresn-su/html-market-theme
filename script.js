// Sidenav
$(document).ready(() => {
    $('.sidenav').sidenav()
})

// Carousel
$('.carousel.carousel-slider').carousel({
    fullWidth: true
});


// Collapsible
$(document).ready(function(){
    $('.collapsible').collapsible();
  });